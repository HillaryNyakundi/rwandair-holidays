import PackageDeals from "../sections/PopularPack";

const Deals = () => {
  return (
    <section id="deals" className="min-h-screen pt-[4rem]">
      <PackageDeals />
    </section>
  );
};

export default Deals;
